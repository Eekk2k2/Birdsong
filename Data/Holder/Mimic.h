#pragma once

#include "Identifier.h"

class Mimic
{
	template <typename... Args> Identifier AidInHelpingObject(Args&&... args) {
		
	}

	template <typename T, typename... Args> Identifier AidInHelpingMaterial(Args&&... args) {
		
	}

	template <typename... Args> Identifier AidInHelpingMesh(Args&&... args) {

	}
};

